# TimeLapse Edge — base requirements for Docker image
# opencv-python-headless installeres IKKE her — det er tungt og device-specifikt.
# Det installeres post-flash på selve OrangePi hardwaren via:
#   /opt/timelapse/venv/bin/pip install opencv-python-headless
# eller via en OS-bundle update fra headend.

pyyaml>=6.0
requests>=2.31.0
paramiko>=3.3.0
cryptography>=41.0

# cffi 2.0.0 har ingen pre-built armhf wheels og kræver gcc til cross-kompilering.
# Pin til 1.x som har manylinux armhf wheels klar til brug på arm32v7 og arm64.
cffi>=1.14,<2.0

# pynacl: transitiv dep fra paramiko. Nyere versioner (>=2.x) har AEGIS AEAD bindings
# (crypto_aead_aegis128l_*) der kræver libsodium >= 1.0.19.
# Debian Trixie har libsodium 1.0.18 — AEGIS mangler → compile fejl med SODIUM_INSTALL=system.
# Pin til 1.5.x som kun kræver libsodium >= 1.0.16.
pynacl==1.5.0

# tzdata for zoneinfo support
tzdata>=2024.1

# Technician authentication (QR code login)
qrcode>=7.4.0

# Local management portal (edge/scripts/totp-service.py — FastAPI/uvicorn HTTPS
# service on port 8443, TOTP login via pyotp, /verify uses Form() which needs
# python-multipart). Pinned to match headend/requirements.txt.
fastapi==0.136.1
uvicorn[standard]==0.46.0
pyotp==2.9.0
python-multipart==0.0.27
